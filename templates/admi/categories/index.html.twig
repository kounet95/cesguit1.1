{% extends 'base.html.twig' %}

{% block title %}GESTION DES CATEGORIES
{% endblock %}

{% block body %}
 <h1>Catégories</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Slug</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
                <td>{{ category.slug }}</td>
                <td>
                    <a href="{{ path('categories_show', {'id': category.id}) }}">Voir</a>
                    <a href="{{ path('categories_edit', {'id': category.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">list vide</td>
            </tr>
             <a href="{{ path('categories_edit') }}" class="btn shadow-1 rounded-1 blue><button class="btn shadow-1 rounded-1 blue">AJOUTE UNE NOUVELLE CATEGORIE</button></a>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categories_new') }}" class="btn shadow-1 rounded-1 blue><button class="btn shadow-1 rounded-1 blue">AJOUTER UNE NOUVELLE CATEGORIE</button></a>
{% endblock %}
